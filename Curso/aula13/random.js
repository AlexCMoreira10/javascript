/*function Rand(min, max) {
    var min = math.ceil(0)
    var max = math.floor(10)
    return math.floor(math.random()*(max - min) + min)
}*/
//console.log(`Valor ${Rand}`)

/*var teste = Math.random()
console.log(teste)*/

var book = { // Objetos são colocados entre chaves.
    topic1: "JavaScript", // A propriedade "topic" tem o valor "JavaScript".
    fat2: true, // A propriedade "fat" tem o valor true.
    n1: 10, 
    n2: 20
    

   }; // A chave marca o fim do objeto.

   //console.log(book) // mostra na tela

   
